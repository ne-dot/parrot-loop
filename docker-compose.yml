services:
  redis:
    image: redis:7-alpine
    ports:
      - '6380:6379'
    volumes:
      - loop_redis_data:/data
    command: ['redis-server', '--appendonly', 'yes']

volumes:
  loop_redis_data:
